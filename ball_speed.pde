// function to call when the paddle size increase
void ball_speed(float x){
  speedSetup=x;
  //println(speedSetup);
  speedX = random(-3,3)+speedSetup; //random(-3, speed);
  speedY = random(-3,3)+speedSetup;//random(-3, speed);*/
  println("here");
  //noLoop();
}
